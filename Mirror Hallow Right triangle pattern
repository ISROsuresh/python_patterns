n = int(input("Enter Number: "))
left_space = " " * 2 * (n - 1)
print(left_space + "*")

for i in range(1, n - 1):
    space = " " * 2 * (n - i - 1)
    m_space = " " * (2 * (i) - 1)
    print(space + "*" + m_space + "*")
print("* " * n)
